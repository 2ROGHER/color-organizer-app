import { Injectable } from '@angular/core';
import Color from '../../models/domain/color.model';
import { IAction } from '../../models/interfaces/action.inteface';
// import { SET_COLOR_STATUS } from '../actions-types/color';

@Injectable()
export class ColorActions {
  // addColorStatus(id: string, s: string): IAction<string> {
  //   return {
  //     type: SET_COLOR_STATUS,
  //     payload: s,
  //   };
  // }
}
