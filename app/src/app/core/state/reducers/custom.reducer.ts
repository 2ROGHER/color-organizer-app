// src/app/state/filter.reducer.ts
// import { createReducer, on } from '@ngrx/store';
// import { setSearchFilter, setStatusFilter } from './filter.actions';
// import { IFilter } from '../../models/interfaces/filter.interface';

// export const initialFilterState: IFilter = { searchTerm: '', colorStatus: 'all', sortBy: '' };

// export const filterReducer = createReducer(
//   initialFilterState,
//   on(setSearchFilter, (state, { search }) => ({ ...state, search })),
//   on(setStatusFilter, (state, { status }) => ({ ...state, status }))
// );
